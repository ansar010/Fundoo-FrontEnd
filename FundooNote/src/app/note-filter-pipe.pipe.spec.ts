import { NoteFilterPipePipe } from './note-filter-pipe.pipe';

describe('NoteFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
